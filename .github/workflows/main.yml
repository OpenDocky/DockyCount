name: Auto Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'

    - name: Read and bump version
      id: version
      run: |
        set -e
        VERSION_FILE="vertion.json"

        if [ ! -f $VERSION_FILE ]; then
          echo '{"vertion":"V0.9.9"}' > $VERSION_FILE
          echo "$VERSION_FILE created with version V0.9.9"
        fi

        version=$(node -pe "require('./$VERSION_FILE').vertion")
        echo "Current version: $version"

        version_numbers=${version#V}
        IFS='.' read -r major minor patch <<< "$version_numbers"

        patch=$((patch + 1))
        if [ $patch -gt 9 ]; then
          patch=0
          minor=$((minor + 1))
        fi
        if [ $minor -gt 9 ]; then
          minor=0
          major=$((major + 1))
        fi

        new_version="V$major.$minor.$patch"
        echo "New version: $new_version"

        node -e "const fs = require('fs'); fs.writeFileSync('$VERSION_FILE', JSON.stringify({ vertion: '$new_version' }, null, 2));"

        echo "new_version=$new_version" >> $GITHUB_ENV
        echo "::set-output name=new_version::$new_version"
        
    - name: Commit new version
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add vertion.json
        git diff --cached --quiet || git commit -m "Bump version to ${{ env.new_version }}"
        git push https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/${{ github.repository }} HEAD:main

    - name: Create GitHub Release
      uses: ncipollo/release-action@v1
      with:
        tag: ${{ env.new_version }}
        name: ${{ env.new_version }}
        body: "Automated release for ${{ env.new_version }}"
        draft: false
        prerelease: false
        makeLatest: true
